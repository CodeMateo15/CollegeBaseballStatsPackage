# Every package here must appear in docs/conf.py `extensions` (or be the theme),
# otherwise it is installed on every docs build and does nothing. furo was
# listed for a long time while html_theme was sphinx_rtd_theme.
sphinx>=7.0.0
sphinx_rtd_theme>=1.3.0
myst-parser>=2.0.0
sphinx-copybutton>=0.5.2
matplotlib>=3.9
